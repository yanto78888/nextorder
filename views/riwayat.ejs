<!DOCTYPE html>
<html lang="id">
<head><%- include('partials/head') %></head>
<body>
<div class="app-shell">
  <%- include('partials/user-sidebar', { active: 'riwayat' }) %>

  <div class="main">
    <div class="topbar"><h2>Riwayat Order</h2></div>

    <% if (success) { %><div class="alert success"><%= success %></div><% } %>

    <div class="card">
      <% if (orders.length === 0) { %>
        <div class="empty-state">Belum ada riwayat order.</div>
      <% } else { %>
      <table>
        <thead><tr><th>ID</th><th>Produk</th><th>Qty</th><th>Total</th><th>Mode</th><th>Status</th><th>Detail</th><th>Tanggal</th></tr></thead>
        <tbody>
          <% orders.forEach(o => { %>
          <tr>
            <td class="mono"><%= o.id %></td>
            <td><%= o.productName %></td>
            <td><%= o.qty %></td>
            <td class="mono">Rp <%= o.total.toLocaleString('id-ID') %></td>
            <td><span class="badge <%= o.deliveryMode === 'auto' ? 'completed' : 'admin' %>"><%= o.deliveryMode || o.source %></span></td>
            <td><span class="badge <%= o.status %>"><%= o.status %></span></td>
            <td style="max-width:260px; font-size:12px; color:var(--text-dim); white-space:pre-wrap;"><%= o.detail || (o.manualRequired ? 'Menunggu admin kirim manual' : '-') %></td>
            <td><%= new Date(o.createdAt).toLocaleString('id-ID') %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <% } %>
    </div>
  </div>
</div>
</body>
</html>
